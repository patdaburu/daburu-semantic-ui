{
  "name": "daburu-semantic-ui",
  "version": "1.0.0",
  "description": "This package contains the distributables from the Semantic-UI configuration project.",
  "main": "index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "gulp build --cwd semantic",
    "clean:dist": "rm -rf dist",
    "copy:dist": "mkdir -p dist && cp -r semantic/dist/* dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patdaburu/daburu-semantic-ui.git"
  },
  "keywords": [
    "css",
    "semantic-ui"
  ],
  "author": "Pat Daburu",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/patdaburu/daburu-semantic-ui/issues"
  },
  "homepage": "https://github.com/patdaburu/daburu-semantic-ui#readme",
  "devDependencies": {
    "gulp": "^3.9.1",
    "semantic-ui": "^2.2.13"
  },
  "dependencies": {

  }
}
